<pages:BasePage x:Class="SeaBattleWPF.Pages.GamePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:gameControls="clr-namespace:SeaBattleWPF.GameControls"
      xmlns:pages="clr-namespace:SeaBattleWPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="GamePage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="0.3*"></RowDefinition>
            <RowDefinition Height="4*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>


        <TextBlock Grid.Row ="0"  Grid.Column="1" Style="{StaticResource StyledTextBlock}" Text="You"></TextBlock>

        <gameControls:Field  Grid.Row ="1"  Grid.Column="1" Margin="10" x:Name="Computer">

        </gameControls:Field>

        <TextBlock Grid.Row ="0"  Grid.Column="0" Style="{StaticResource StyledTextBlock}" Text="Enemy"></TextBlock>

        <gameControls:Field Grid.Row ="1"  Grid.Column="0" Margin="10" x:Name="Player">

        </gameControls:Field>

        <Frame x:Name="Chat" NavigationUIVisibility="Hidden" Source="ChatPage.xaml" Grid.ColumnSpan="2" Grid.Row="2"></Frame>
    </Grid>
</pages:BasePage>

